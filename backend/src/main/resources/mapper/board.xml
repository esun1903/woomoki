<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.project.dao.BoardDao">
	<select id="list" resultType="com.ssafy.project.dto.BoardDto">
		SELECT BOARDID, USERSEQ, TITLE, CONTENT, REGDATE
		FROM BOARD
		ORDER BY REGDATE DESC
		LIMIT 20
	</select>
	
	<select id="detail" resultType="com.ssafy.project.dto.BoardDto" parameterType="String">
		SELECT BOARDID, USERSEQ, TITLE, CONTENT, REGDATE
		FROM BOARD
		WHERE BOARDID = #{id}
	</select>
	
	<insert id="write" parameterType="com.ssafy.project.dto.BoardDto">
		INSERT 
		INTO BOARD (USERSEQ, TITLE, CONTENT) 
		VALUES (1, #{title}, #{content})
	</insert>
</mapper>