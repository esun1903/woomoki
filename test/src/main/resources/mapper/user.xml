<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.project.dao.UserDao">

	<select id="login" parameterType="com.ssafy.project.dto.UserDto" resultType="com.ssafy.project.dto.UserDto">
		SELECT
		USERNAME, USERID, EMAIL, ADDRESS, JOINDATE, usertype
		FROM USER
		WHERE USERID = #{userid}
		 AND USERPWD = #{userpwd}
	</select>

	<insert id="register" parameterType="com.ssafy.project.dto.UserDto">
		INSERT 
		INTO USER 
		(USERID, USERNAME, USERPWD, EMAIL)
		VALUES
		(#{userid} ,#{username}, #{userpwd}, #{email})
	</insert>

</mapper>